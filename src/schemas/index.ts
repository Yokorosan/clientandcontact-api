import {
  client,
  clientCreate,
  clientResponse,
  clientEdit,
} from "./client.schema";
import { registerContact, registerWithUser } from "./contact.schema";
import { login } from "./login.schema";

export {
  client,
  clientCreate,
  clientResponse,
  clientEdit,
  registerContact,
  registerWithUser,
  login,
};
